{
	"name": "vite-project",
	"private": true,
	"version": "0.0.0",
	"type": "module",
	"scripts": {
		"dev": "vite",
		"build": "vite build --emptyOutDir",
		"preview": "vite preview",
		"deploy": "cd ../dist && git add . && git commit -m Build && git push origin gh-pages && cd ../project"
	},
	"devDependencies": {
		"@fullhuman/postcss-purgecss": "^5.0.0",
		"postcss": "^8.4.21",
		"sass": "^1.61.0",
		"vite": "^4.2.0"
	}
}
