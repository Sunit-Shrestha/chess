{
	"name": "vite-project",
	"private": true,
	"version": "0.0.0",
	"type": "module",
	"scripts": {
		"dev": "vite",
		"build": "vite build",
		"preview": "vite preview",
		"add": "git add ",
		"commit": "git commit -m 'Build.'",
		"push": "git push origin main",
		"deploy": "npm run add && npm run commit && npm run push"
	},
	"devDependencies": {
		"@fullhuman/postcss-purgecss": "^5.0.0",
		"postcss": "^8.4.21",
		"sass": "^1.61.0",
		"vite": "^4.2.0"
	}
}
